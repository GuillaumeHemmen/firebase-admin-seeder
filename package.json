{
  "name": "firebase-admin-seeder",
  "version": "1.0.0",
  "description": "CLI for seeding Firebase users with admin role via custom claims",
  "main": "dist/src/seedAdmin.js",
  "types": "dist/src/seedAdmin.d.ts",
  "bin": {
    "seed-admin": "dist/bin/seed-admin.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "test": "TZ=UTC jest --coverage --updateSnapshot --maxWorkers=75%",
    "prepare": "npm run build && npm run lint && npm test"
  },
  "keywords": [
    "firebase",
    "admin",
    "cli",
    "seed",
    "custom-claims"
  ],
  "author": "Guillaume Van Hemmen <guillaume [at] van [dash] hemmen.com",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "eslint-plugin-node": "^11.0.0"
  }
}
